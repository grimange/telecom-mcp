# Changes Made

## Scope in This Run

- This run executed the remediation pipeline against audit set `20260307T081209Z` and validated remediation code/tests already present in the current workspace.
- No additional source-code edits were required in this run to close `DR-001`..`DR-004`.

## Remediated Files Present in Workspace

- `src/telecom_mcp/tools/asterisk.py`
- `src/telecom_mcp/normalize/asterisk.py`
- `src/telecom_mcp/connectors/asterisk_ami.py`
- `src/telecom_mcp/mcp_server/server.py`
- `docs/tools.md`
- `tests/test_connectors.py`
- `tests/test_expansion_batch2_tools.py`
- `tests/test_remediation_hardening.py`

## Behavior Coverage

1. `DR-001` AMI registration action correction
- Uses documented `PJSIPShowRegistrationsOutbound` and filters event-list results by registration identity.

2. `DR-002` AMI channel detail correction
- Uses documented `CoreShowChannels` action and derives channel detail from `CoreShowChannel` event entries.

3. `DR-003` AMI command framing hardening
- Command response completeness and raw `Output:` extraction cover fragmented/multi-frame response patterns.

4. `DR-004` Empty-contact semantics
- `No Contacts found` is normalized to an empty dataset with `data_quality` caveat rather than hard error.

5. Local contract override documentation
- `asterisk.health` dual-plane AMI+ARI requirement is explicitly documented.

## Rollback Concerns

- Medium: connector command-framing behavior affects multiple CLI/module wrappers; rollback risks regressions.
- Low: action-name corrections align with documented AMI contracts.
