# Module Posture Expansion

## Implemented
- Added `asterisk.modules` and `freeswitch.modules` tools.
- Integrated vendor module inventory into `telecom.inventory` posture/baseline fields.
- Updated MCP SDK wrappers, tool contract tests, and examples/docs.

## Validation
- `pytest -q` passed.
