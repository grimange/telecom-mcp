# Tool Catalog v2 (Proposed + Batch 1 Implemented)

Timestamp: `20260306T095113Z`

## Layer 1: Vendor-neutral telecom.*

### Implemented Batch 1

- `telecom.endpoints(pbx_id, filter?, limit?)`
  - Purpose: unified endpoint availability view.
  - Class: Read.
  - Output: normalized envelope with `pbx_id`, `platform`, `summary`, `items`, `counts`, `warnings`, `truncated`, `captured_at`.
  - Failure modes: `VALIDATION_ERROR`, upstream tool failures.
  - Domain: troubleshooting, usability.

- `telecom.registrations(pbx_id, limit?)`
  - Purpose: unified registration state view.
  - Class: Read.
  - Failure modes: `VALIDATION_ERROR`, upstream tool failures.
  - Domain: troubleshooting, auditing.

- `telecom.channels(pbx_id, limit?)`
  - Purpose: unified active-channel list.
  - Class: Read.
  - Failure modes: `VALIDATION_ERROR`, upstream tool failures.
  - Domain: troubleshooting.

- `telecom.calls(pbx_id, limit?)`
  - Purpose: unified active-call list.
  - Class: Read.
  - Failure modes: `VALIDATION_ERROR`, upstream tool failures.
  - Domain: troubleshooting.

- `telecom.logs(pbx_id, grep?, tail?, level?)`
  - Purpose: unified bounded log retrieval.
  - Class: Read.
  - Failure modes: `VALIDATION_ERROR`, `NOT_FOUND` (no configured log source).
  - Domain: troubleshooting, auditing.

- `telecom.inventory(pbx_id)`
  - Purpose: target baseline inventory summary.
  - Class: Read.
  - Failure modes: upstream source failures.
  - Domain: auditing, usability.

### Planned/deferred

- `telecom.diff_snapshots(snapshot_a, snapshot_b)`
- `telecom.run_smoke_test(pbx_id)`
- `telecom.assert_state(pbx_id, assertion, params?)`
- `telecom.compare_targets(pbx_a, pbx_b)`

## Layer 2: Vendor-specific deep tools

### Implemented Batch 1

- `asterisk.pjsip_show_contacts(pbx_id, filter?, limit?)`
- `asterisk.version(pbx_id)`
- `asterisk.logs(pbx_id, grep?, tail?, level?)`
- `freeswitch.version(pbx_id)`
- `freeswitch.logs(pbx_id, grep?, tail?, level?)`

### Planned/deferred

- `asterisk.cli` allowlisted read commands
- `freeswitch.api` allowlisted read commands
- `asterisk.core_show_channel` deep wrapper
- `freeswitch.channel_details`
- modules inventory tools

## Layer 3: Safe validation tools

Deferred. No active probe/originate mutation tools were added in this run.

## Notes

- Existing v1 tools are preserved.
- New tools are additive and inspect-mode safe.
- Write tool contract remains unchanged.
